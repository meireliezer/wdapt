import { ActionFormatterPipe } from './action-formatter.pipe';

describe('ActionFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
