import { MyModalDirective } from './my-modal.directive';

describe('MyModalDirective', () => {
  it('should create an instance', () => {
    const directive = new MyModalDirective();
    expect(directive).toBeTruthy();
  });
});
